{
  "name": "@scp/plugin-webhook-control",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@scp/plugin-api": "workspace:*"
  },
  "devDependencies": {
    "@scp/plugin-testkit": "workspace:*",
    "typescript": "~5.8.0",
    "vitest": "^3.0.0"
  }
}
