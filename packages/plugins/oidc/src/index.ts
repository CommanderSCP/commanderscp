/**
 * @scp/plugin-oidc — stub scaffold (M0 walking skeleton).
 *
 * Empty-but-compiling per BUILD_AND_TEST.md §2 / §8 M0. Real implementation lands in M2 (generic OIDC identity plugin)
 * (see docs/BUILD_AND_TEST.md §8 for the milestone plan, docs/DESIGN.md §3 for this package's
 * role in the repo layout).
 */
export const STUB = true as const;
