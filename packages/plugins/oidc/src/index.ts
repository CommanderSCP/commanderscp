/**
 * @scp/plugin-oidc — stub scaffold (M0 walking skeleton).
 *
 * Empty-but-compiling per BUILD_AND_TEST.md §2 / §8 M0. SUPERSEDED: M2 shipped OIDC in-server (apps/server/src/auth/oidc.ts), not as this plugin; this package is a retained empty scaffold (nothing imports it as functional) — kept for the walking-skeleton package layout, safe to delete in a future cleanup
 * (see docs/BUILD_AND_TEST.md §8 for the milestone plan, docs/DESIGN.md §3 for this package's
 * role in the repo layout).
 */
export const STUB = true as const;
