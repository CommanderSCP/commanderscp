# Helm chart

Placeholder per the binding repo layout (DESIGN.md §3). The chart (api/worker Deployments,
migrations Job pre-upgrade hook, optional runner-iac Job template, hardened defaults,
NetworkPolicies, ServiceMonitor) lands in **M8** (BUILD_AND_TEST.md §8). Until then this
directory exists only to hold the layout in place — M0 ships evaluation via
`deploy/compose/` only.
